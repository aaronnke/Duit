<%= form_for :new_expense, url: budget_create_new_expense_path(@budget[index].id), method: :post  do |f| %>
  <%= f.text_field :tag %>
  <%= f.number_field :amount %>
  <%= f.submit "Add" %>
<% end %>
